function r = fixedpointit(g, eps, N)
%This function finds a root to arbitrary precision using the
%fixed point iteration method
%   g:      fixed point scheme of function
%   eps:    tolerance 
%   N:      iteration limit
%

%------------------Prev Version------------%

%p_p : Previous point
p_p = .9;
p_n = g(p_p);

counter = 1;
while ((abs(p_n - p_p) > eps) && N >= counter)    
    p_p = p_n;
    p_n = g(p_n);    
    counter = counter +1;
end 
r = p_n;

